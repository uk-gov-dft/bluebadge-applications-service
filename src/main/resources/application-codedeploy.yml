blue-badge:
  auth-server: 
    url: https://api.${bb_env}.${url}/bb_auth
  reference-data-service:
    service-host:
      host: api.${bb_env}.${url}
      port: 443
      scheme: https
      contextpath: /reference-data1
  message-service:
    service-host:
      host: api.${bb_env}.${url}
      port: 443
      scheme: https
      contextpath: /messages1

management:
  server:
    port: ${bb_health:-8081}

spring:
  main:
    banner-mode: "off"
  datasource:
     ***REMOVED***
    username: ${bb_db_username:-developer}
    url: jdbc:postgresql://${RDS_HOSTNAME}:5432/${bb_database:-bb_dev}?currentSchema=applicationmanagement&sslmode=verify-full

server:
  port: ${bb_port:-8080}

amazon:
  s3bucket: ${bb_application_artifact_bucket}
